using System.IO;

namespace NiDotNet.NIF.Nodes
{
    public class NiInterpolator : NiObject
    {
        public NiInterpolator(BinaryReader reader, NiFile niFile) : base(reader, niFile)
        {
        }
    }
}