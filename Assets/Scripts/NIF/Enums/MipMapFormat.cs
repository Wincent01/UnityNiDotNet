namespace NiDotNet.NIF.Enums
{
    public enum MipMapFormat : uint
    {
        MipFmtNo, // Texture does not use mip maps.
        MipFmtYes, // Texture uses mip maps.
        MipFmtDefault // Use default setting.
    }
}